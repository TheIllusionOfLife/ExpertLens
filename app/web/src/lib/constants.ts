/** Coach IDs that are built-in presets — cannot be deleted, match backend KNOWN_PRESET_IDS. */
export const PRESET_COACH_IDS = new Set([
  "blender",
  "affinity_photo",
  "unreal_engine",
  "fusion",
  "zbrush",
]);
